import { Route } from '@angular/router';
import { HeaderComponent } from './header/preview/header.component';

export const appRoutes: Route[] = [
  {
    path: '',
    component: HeaderComponent
  }
];
